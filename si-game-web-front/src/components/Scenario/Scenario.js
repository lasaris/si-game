import React from 'react';

const scenario = () => {
    return (
    <div>
        foreach(var scenarioModule in scenarioModules)
            render each ScenarioModule
    </div>);
};

export default scenario;